import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1
    },
    confirmedContainer: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      flexGrow: 0.4
    },
    confirmedOutput: {
        fontSize: 17,
        fontWeight: 'bold',
        color: '#000',
    }
  });
